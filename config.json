// Файл конфигурации V2Ray. Этот файл соответствует стандартному формату JSON с поддержкой комментариев.
// Откомментируйте записи ниже, чтобы удовлетворить ваши потребности. Также прочитайте наше руководство для более подробной информации на
// https://www.v2ray.com /
{
  «log»: {
 // По умолчанию V2Рэй пишет журнал доступа к stdout.
 // «доступ»: "/ path / to / access / log / file",

 // По умолчанию V2Рэй пишет журнал ошибок в stdout.
 // «Ошибка»: "/ path / to / error / log / file",

 // Уровень журнала, один из «Отладка», «Информация», «Предупреждение», «Ошибка», "Никто"
 "Loglevel" : «Предупреждение»
  },
 // Список конфигураций входящих прокси.
  "inbounds": [{
 // Порт для прослушивания. Вам может понадобиться корневой доступ, если значение меньше 1024.
    «порт»: 1080,

 // IP-адрес для прослушивания. Изменить на «0.0.0.0» прослушивать все сетевые интерфейсы.
 "Слушать": «127.0.0.1»,

 // Метка входящего прокси. Может использоваться для маршрутизации.
    «Тег»: "носки-входящие",

 // Протокол имени входящего прокси.
    «Протокол»: «носки»,

 // Настройки протокола. Зависит от протокола.
    «Посещения»: {
      "Auth": «Ноут»,
      «Удп»: ложный,
      «ip»: «127.0.0.1»
    },

 // Включить обнюхивание по TCP-соединению.
    «нюхает»: {
      «включено»: правда,
 // Целевой домен будет переопределен на тот, который несет соединение, если соединение HTTP или HTTPS
      "destOverride": [«Http», "TLS"]
    }
  }],
 // Список конфигураций исходящих прокси.
  "преимущества": [{
 // Название протокола исходящего прокси.
    «Протокол»: «свобода»,

 // Настройки протокола. Зависит от протокола.
    «Посещения»: {},

 // Метка исходящего. Может использоваться для маршрутизации.
    «Тег»: «прямой»
  },{
    «Протокол»: «черная дыра»,
    «Посещения»: {},
    «Тег»: «заблокировано»
  }],

 // Транспорт для глобальных транспортных условий. Если у вас есть несколько транспортов с одинаковыми настройками
 // (скажем, mKCP), вы можете поместить это здесь, а не в каждом отдельном входе / выходе.
 // «Транспорт»: {},

 // Маршрутизация контролирует, как трафик с inbounds отправляется на outbounds.
  «Маршрутизация»: {
    "DomainStrategy": «IPOnDemand»,
    «Правила»:[
      {
        // Blocks access to private IPs. Remove this if you want to access your router.
        "type": "field",
        "ip": ["geoip:private"],
        "outboundTag": "blocked"
      },
      {
        // Blocks major ads.
        "type": "field",
        "domain": ["geosite:category-ads"],
        "outboundTag": "blocked"
      }
    ]
  },

  // Dns settings for domain resolution.
  "dns": {
    // Static hosts, similar to hosts file.
    "hosts": {
      // Match v2ray.com to another domain on CloudFlare. This domain will be used when querying IPs for v2ray.com.
      "domain:v2ray.com": "www.vicemc.net",

      // The following settings help to eliminate DNS poisoning in mainland China.
      // It is safe to comment these out if this is not the case for you.
      "domain:github.io": "pages.github.com",
      "domain:wikipedia.org": "www.wikimedia.org",
      "domain:shadowsocks.org": "electronicsrealm.com"
    },
    «серверы»: [
      «1.1.1.1»,
      {
        «адрес»: «114.114.114.114»,
        «порт»: 53,
 // Список доменов, которые сначала используют этот DNS.
        «домены»: [
          "geosite: cn"
        ]
      },
      «8.8.8.8»,
      «localhost»
    ]
  },

 // Политика контролирует некоторое внутреннее поведение того, как V2Рэй обрабатывает соединения.
 // Это может быть на уровне соединения по уровням пользователя в «уровнях» или по глобальным настройкам в «системе».«
  «политика»: {
 // Политика подключения по уровням пользователей
    «Уровни»: {
      «0»: {
        "uplinkOnly": 0,
        "downlinkOnly": 0
      }
    },
    «система»: {
      "statsInboundUplink": ложный,
      "statsInboundDownlink": ложный,
      "statsOutboundUplink": ложный,
      "statsOutboundDownlink": ложный
    }
  },

 // Статистика позволяет внутренний счетчик статистики.
 // Этот параметр можно использовать вместе с Policy и Api. 
 //«Статы»:{},

 // Api позволяет API-интерфейсам gRPC для внешних программ связываться с V2Пример луча.
 //«Апи»: {
 //«Тег»: «Апи»,
 //«Услуги»: [
 // "HandlerService",
 // "LoggerService",
 // "StatsService"
 // ]
 // },

 // Вы можете добавить другие записи в конфигурацию, но они не будут распознаны V2Ray.
  "другой": {}
}
